google-auth==2.35.0
PyJWT==2.9.0
requests==2.32.3
# Pinned (rather than relying on the Lambda runtime's bundled boto3) because the
# Bedrock Runtime Converse API used by classify_phishing_email needs boto3>=1.34.116.
boto3==1.35.36
